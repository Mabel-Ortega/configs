server:
  port: 8086
  
spring:
  data:
    mongodb:
      authentication-database: admin
      username: root
      password: root
      database: final_be
      port: 27017
      host: localhost

info:
  app:
    name: ${spring.application.name}
    version: 1.0.0
    description: Series Microservice
